# ADR-0004：系统凭据库默认、SQLite 保存引用

- 状态：提议，随本批设计评审
- 日期：2026-08-02

## 决策

定义 CredentialStore；Windows V1 默认使用系统凭据库；SQLite 只保存 credential_ref 和掩码提示；Local Access Key 只保存哈希；用户无需主密码；V1 不实现明文便携模式。

## 边界与代价

该方案降低数据库、备份和误分享风险，但不能防御已经控制当前用户进程的恶意软件。跨机备份不包含秘密；macOS/Linux 需各自实现；数据库与凭据写入需要补偿事务；测试使用内存实现。